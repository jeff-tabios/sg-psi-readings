platform :ios, '10.0'

target 'SG PSI Readings' do
  
  inhibit_all_warnings!
  use_frameworks!

  # Pods for SG PSI Readings
  pod 'Networking', '~> 5.0'

  target 'SG PSI ReadingsTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Quick', '~> 2.1'
    pod 'Nimble', '~> 8.0'
  end

end
